create database chaskaeats;
use chaskaeats;
CREATE TABLE userr (uname VARCHAR(50), email VARCHAR(20), mobile_no bigint(10), dob DATE , memType VARCHAR(50), pass VARCHAR(50));

SELECT * FROM chaskaeats.userr;
use chaskaeats;
select * from userr;